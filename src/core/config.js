'use strict';
const config = {
  CONSTANTS: {
    MESSAGES: {
      ERROR: {
        MISSING_OVERIDE: 'Method has not been implemented!',
      },
    },
  },
};
Object.freeze(config);

module.exports = config;
